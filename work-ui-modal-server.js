const http=require('http');
const {spawn}=require('child_process');
const aiPort=3499;
const s=http.createServer((req,res)=>{let b='';req.on('data',c=>b+=c);req.on('end',()=>{const p=JSON.parse(b||'{}');const sys=String(p.messages?.[0]?.content||'');const last=String(p.messages?.[p.messages.length-1]?.content||'');let x={};if(last.includes('分析下面的代码片段'))x={explanation:'这一行把单价和数量相乘，得到总价。',file:'示例代码',confidence:'高',risk_level:'低',risk_reason:'没有明显风险。',alternative_code:'const total = price * count;'};else if(last.includes('逐行') || last.includes('这一行'))x={plain_explanation:'先读取单价和数量，再相乘赋给总量。',analogy:'像把单价和件数交给收银员。',tokens:[{text:'price',meaning:'商品单价'},{text:'count',meaning:'购买数量'},{text:'total',meaning:'总价'}],execution_before:'已有单价和数量。',execution_after:'总量得到乘积。',why_here:'完成总价计算。',if_wrong:'变量写错会找不到值。',must_know:['乘法计算总价。']};else if(last.includes('理解'))x={correct_parts:['你知道这一行在算总价。'],missing_parts:['可以补充变量的含义。'],wrong_parts:[],guiding_question:'如果数量变成 0，总价会是多少？',standard_explanation:'单价乘数量得到总价。',encouragement:'你抓住了核心。'};else if(last.includes('练习'))x={correct_parts:['思路正确。'],missing_parts:[],wrong_parts:[],encouragement:'很好。',standard_explanation:'改变变量会重新计算。'};else x={answer:'因为乘法需要两个数。',correction:'',key_takeaway:'先看变量代表什么。'};res.writeHead(200,{'Content-Type':'application/json'});res.end(JSON.stringify({choices:[{message:{content:JSON.stringify(x)}}]}));});});
s.listen(aiPort,'127.0.0.1',()=>{console.log('FAKE_AI_'+aiPort);});
const srv=spawn(process.execPath,['server/src/index.js'],{cwd:'F:/AI-codex/daimaxuexi',env:{...process.env,PORT:'3501',LEARNING_DB_PATH:'F:/AI-codex/daimaxuexi/data/ui-modal.db'},stdio:['ignore','pipe','pipe']});
srv.stdout.on('data',d=>process.stdout.write('SRV '+d));srv.stderr.on('data',d=>process.stderr.write('ERR '+d));
setInterval(()=>{},1000);
