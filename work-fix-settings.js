const fs=require('fs');const p='client/src/components/SettingsPanel.jsx';let lines=fs.readFileSync(p,'utf8').split(/\r?\n/);const idx=lines.findIndex((l,i)=>i>38&&l.trim()==='}');if(idx<0)throw new Error('orphan brace not found');lines.splice(idx,0,'  async function deleteAccount() {');fs.writeFileSync(p,lines.join('\n'),'utf8');console.log('inserted at',idx+1);
